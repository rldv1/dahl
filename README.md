# Dahl
Здесь будут статьи, разборы, исследования и различные PoC.

## poc.py
Проверяет, использует ли пользователь мессенджер **Telega (Dahl)**

При реверсе Android-версии клиента я обнаружил что при P2P звонке Telega создаёт фиктивный вызов через собственный REST API -> `POST /api/calls/create`

```bash
python poc.py user_id
# {"ok": true, "dahl_status": 2}
```

`dahl_status`: `0` = не использовал, `1` = нет активной сесии, `2` = есть активная сессия
#### Важное уточнение: на момент публикации PoC - он уже был исправлен со стороны сервера, gg :v
